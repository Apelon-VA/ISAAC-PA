<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.2"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.2 http://maven.apache.org/xsd/assembly-1.1.2.xsd">
    <formats>
        <format>zip</format>
    </formats>
    <fileSets>
        <fileSet>
            <directory>${project.basedir}/src/main/scripts</directory>
            <outputDirectory>/</outputDirectory>
        </fileSet>
    </fileSets>
    <dependencySets>
        <dependencySet>
            <outputDirectory>lib</outputDirectory>
            <useProjectArtifact>true</useProjectArtifact>
            <!-- Maven is broken (I know, you are shocked) and doesn't properly evaluate scopes on dependencies when the scope
            is overridden in a child pom.  Sigh.  So, include all test, then exclude the test ones we don't actually need. -->
            <scope>test</scope>
            <excludes>
                <exclude>junit:junit</exclude>
                <exclude>org.ihtsdo.otf:tcc-test-data</exclude>
            </excludes>
        </dependencySet>
    </dependencySets>
    <includeBaseDirectory>false</includeBaseDirectory>
</assembly>
